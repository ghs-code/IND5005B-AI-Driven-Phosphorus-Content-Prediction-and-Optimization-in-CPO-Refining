# Raw Data Placeholder

Do not commit real raw data here.

Place confidential quality-table Excel files in a local ignored directory such
as `local_data/raw/`, then run `make validate-data` or use the browser workbench.
