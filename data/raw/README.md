本目录不存放真实原始数据。

出于保密要求，原始输入文件请仅保存在本地目录 `local_data/raw/`，并且不要提交到 Git 仓库。

`factor_sidecar_template.csv` 是可提交的空模板，用于说明潜在影响因素 sidecar 的字段格式；不要在该模板中填入真实业务数据后提交。

建议的本地文件路径示例：

`local_data/raw/Copy of R3 QUALITY 2025.xlsx`
